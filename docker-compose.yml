services:
  bot:
    build: .
    image: aoabot
    container_name: aoabot
    restart: unless-stopped
    # Mount a host config.yml (with your real token). Create it first:
    #   cp config.example.yml config.yml   # then edit the token
    volumes:
      - ./config.yml:/app/config.yml
